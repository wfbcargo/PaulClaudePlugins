# Gotchas

Non-obvious pitfalls. Append a bullet when something bites you once. The test is:
would a future agent hit this even after reading the code and the diff?

- <"we tried X, it failed because Y">
