# Active Project Rules

Flat list, stable IDs. The orchestrator reads this once at session start and
passes it verbatim under `## ACTIVE RULES` in every spawn prompt. Sub-agents do
not re-read it.

## R-001: <short rule statement>
Added: <ISO date> | Source: decisions/<NNNN>-<slug>.md
<one-paragraph elaboration>
