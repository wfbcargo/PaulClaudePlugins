{
  "name": "claude-boundaries",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Test harness for the claude-boundaries checker. The plugin itself has no dependencies and needs no install to run.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  }
}
